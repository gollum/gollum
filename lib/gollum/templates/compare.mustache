<div id="wiki-wrapper" class="compare">
<div id="head">
  <h1>History for <strong>{{path}}</strong></h1>

  <ul class="actions">
    <li>
      {{>searchbar}}
    </li>
    <li><a href="{{base_url}}/{{escaped_url_path}}"
       class="action-view-page btn btn-sm btn-info">View Page</a></li>
    <li><a href="{{base_url}}/edit/{{escaped_url_path}}"
       class="action-edit-page btn btn-sm btn-info">Edit Page</a></li>
    <li><a href="{{base_url}}/history/{{escaped_url_path}}"
       class="action-page-history btn btn-sm btn-info">Page History</a></li>
  </ul>
</div>

{{#message}}
<p>{{message}}</p>
{{/message}}

<div id="compare-content">

  {{#show_revert}}
  <ul class="actions">
    <li><a href="{{base_url}}/history/{{escaped_url_path}}"
       class="action-page-history btn btn-sm btn-info">Back to Page History</a></li>
    <li>
      <form name="gollum-revert" action="{{base_url}}/revert/{{escaped_url_path}}/{{before}}/{{after}}" method="post" id="gollum-revert-form">
      <a href="#" class="gollum-revert-button btn btn-info btn-sm">Revert Changes</a>
      </form>
    </li>
  </ul>
  {{/show_revert}}

  <div class="data highlight">
    <table cellpadding="0" cellspacing="0">
      {{#lines}}
        <tr>
          <td class="line_numbers">{{ldln}}</td>
          <td class="line_numbers">{{rdln}}</td>
          <td>
            <pre><div class="{{class}}">{{line}}</div></pre>
          </td>
        </tr>
      {{/lines}}
    </table>
  </div>
</div>
<div id="footer">
  <ul class="actions">
    <li><a href="{{base_url}}/history/{{escaped_url_path}}"
       class="action-page-history btn btn-sm btn-info">Back to Page History</a></li>
    {{#show_revert}}
    <li>
      <a href="#" class="gollum-revert-button btn btn-sm btn-info">Revert Changes</a>
    </li>
    {{/show_revert}}
    <li><a href="#">Back to Top</a></li>
  </ul>
</div>
</div>
</div>
